[Unit]
Description=nexus service
After=network.target
  
[Service]
Type=forking
ExecStart={{ nexus_home }}/{{ nexus_app_dir }}/bin/nexus start
ExecStop={{ nexus_home }}/{{ nexus_app_dir }}/bin/nexus stop
User={{ nexus_user }}
Restart=on-abort
  
[Install]
WantedBy=multi-user.target
